allprojects {
    group 'com.jeyrs.spark'
    version '1.0'

    apply plugin: 'java'
    apply plugin: 'idea'
    apply plugin: 'java'

    test {
        testLogging {
            exceptionFormat = 'full'
        }
    }


    task wrapper(type: Wrapper) {
        gradleVersion = '2.4'
    }
}
